{"body":"**状态**：待开始 | **类型**：AFK | **阻塞于**：#7, #8\n\n## What to build\n\n实现两阶段翻译流水线的第二阶段：从 `products.db` 读取结构化产品记录，生成面向 Wildberries 的俄语 SEO 文案，写入 `translations` 表。\n\n端到端行为：\n1. 在现有 `TranslationProvider` 抽象基类新增 `translate_from_record(product_record: dict) -> dict` 方法，接收完整结构化记录而非仅 title + details\n2. 改造 System Prompt（`prompts/translation_persona.txt`）：\n   - 移除\"先分析原文 → 提取卖点\"步骤（Phase 1 已完成）\n   - 改为\"从已提取的属性表中，选取最打动俄罗斯消费者的卖点作为主线\"\n   - 利用 Phase 1 产出的 category 字段实施品类差异化文案策略\n   - 利用 Phase 1 产出的 brand 字段精准去品牌化（只替换确认的品牌词）\n   - 保留蜘蛛网三层关键词布局策略\n3. `run_phase2(asins, progress_callback)` 批量入口：查询 `get_products_pending_phase2()` → 读 products 记录 → 调 AI 生成 → 写入 translations 表\n4. 输出 3 字段不变（russian_title / core_keywords / russian_description），内容更精准、更有品类针对性\n5. CLI 入口：`python phase2_translator.py --db products.db --asin B0XXX`（可选按 ASIN 单条生成）\n\n## Acceptance criteria\n\n- [ ] 用 Phase 1 已萃取的产品记录跑单条，生成完整俄语文案（3 字段非空）\n- [ ] 俄语标题 ≤ 60 字符、全小写、无特殊符号\n- [ ] 品牌词被精准去除（不误删普通词）\n- [ ] 详情中核心功能词以同义词形式自然重复 2-3 次\n- [ ] `get_products_pending_phase2()` 正确排除已有翻译记录的 ASIN\n- [ ] Phase 2 可独立配置 AI 服务商（与 Phase 1 不同时正常使用 Phase 2 配置，未配置时 fallback）\n- [ ] 提供 mock provider 用于测试（不依赖真实 API）\n- [ ] 旧有 `translate(title, details)` 方法保持兼容（图片翻译仍使用旧接口）\n\n## Blocked by\n\n- #7 数据库与配置基础设施\n- #8 Phase 1：AI 信息萃取","title":"#9 Phase 2：AI 文案生成"}
