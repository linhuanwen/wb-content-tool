{"body":"**状态**：待开始 | **类型**：AFK | **阻塞于**：#7, #8, #9, #10\n\n## What to build\n\n清理被 Phase 1 替代的旧代码，适配图片翻译 Tab 到新的数据库架构，确保全量回归测试通过。\n\n端到端行为：\n1. **移除 `extractor.py`**\n   - Phase 1 已从 HTML 直接提取所有字段（包括原标题、图片 URL、详情）\n   - `crawler.py` 不再调用 `extract_product_info()`，仅负责获取 HTML 并存档到 `html/{ASIN}.html`\n   - 爬虫输出改为直接调用 Phase 1 或将 HTML 存档后由用户手动触发 Phase 1\n2. **图片翻译适配**\n   - Tab3 的 `product_context` 从数据库读取（`get_product(asin)` + `get_translation(asin)`），替代从 Excel 字段解析\n   - 兼容场景：Phase 2 未完成时，仅提供 Phase 1 产物（英文属性）作为上下文；Phase 2 完成后，额外提供俄语文案作为翻译参考\n3. **测试套件**\n   - 移除 `extractor.py` 相关的测试用例\n   - Phase 1 和 Phase 2 新模块的单元测试覆盖\n   - 图片翻译适配后的集成测试\n   - 全量 `pytest` 通过\n\n## Acceptance criteria\n\n- [ ] `extractor.py` 相关代码全部移除，无残留引用\n- [ ] `crawler.py` 正常运行（ScraperAPI 和 Playwright 两种模式均 HTML 存档成功）\n- [ ] 图片翻译流程正常：从 DB 读取产品上下文 → 翻译图片文字 → R2 上传\n- [ ] Tab3 在 Phase 2 未完成时仍可正常运行（使用 Phase 1 的英文属性作为上下文）\n- [ ] Tab3 在 Phase 2 完成后使用完整俄语文案作为上下文\n- [ ] 全量 pytest 通过，无 regression\n\n## Blocked by\n\n- #7 数据库与配置基础设施\n- #8 Phase 1：AI 信息萃取\n- #9 Phase 2：AI 文案生成\n- #10 Web 界面适配：两阶段翻译交互","title":"#11 清理与兼容：移除 extractor.py，适配图片翻译"}
