{"body":"**状态**：待开始 | **类型**：AFK | **阻塞于**：#7\n\n## What to build\n\n实现两阶段翻译流水线的第一阶段：将亚马逊产品页 HTML 交给 AI，提取完整的结构化产品属性，写入 `products.db`。\n\n端到端行为：\n1. `InformationExtractor` 抽象基类（ABC），定义 `extract(html: str) -> dict` 抽象方法\n2. 具体实现通过 OpenAI 兼容 API / Anthropic API 调用 AI，传入产品页 HTML + Phase 1 System Prompt\n3. Phase 1 System Prompt（`prompts/phase1_extraction_persona.txt`）：定义角色（电商产品信息萃取专家）和 JSON Schema 输出规范（15 个字段：category, material, color, dimensions, weight, capacity, package_contents, features, technical_specs, target_audience, use_scenarios, unique_selling_points, brand, en_search_keywords）\n4. `run_phase1(asins, html_dir, progress_callback)` 批量入口：遍历 ASIN → 读取 `html/{ASIN}.html` → 调 AI 萃取 → JSON 解析（含重试）→ 写入 products 表\n5. 工厂函数 `create_extractor()` 根据 `PHASE1_API_PROVIDER` 配置选择实例\n6. JSON 解析失败自动重试一次（复用现有 `_parse_ai_response` 风格）\n7. CLI 入口：`python phase1_extractor.py --html-dir html/ --db products.db`\n\n## Acceptance criteria\n\n- [ ] 用真实产品 HTML 跑单条，products 表中各字段非空（至少 category/features/brand/material 有值）\n- [ ] AI 返回的 JSON 缺少必填字段时重试后成功\n- [ ] API Key 未配置时给出明确错误提示，不崩溃\n- [ ] Phase 1 可独立于 Phase 2 运行（萃取完成后 DB 中有记录）\n- [ ] 提供 mock extractor 用于测试（不依赖真实 API）\n\n## Blocked by\n\n- #7 数据库与配置基础设施","title":"#8 Phase 1：AI 信息萃取"}
