{"body":"**状态**：待开始 | **类型**：AFK | **阻塞于**：无\n\n## What to build\n\n建立两阶段 AI 翻译流水线的基础设施：SQLite 数据库 + 拆分 Phase 1/2 的 API 配置。\n\n端到端行为：\n1. 项目根目录下 `products.db` SQLite 数据库，包含两张表：\n   - `products` 表：asin（主键）、title、details、image_urls、html_path、category、material、color、dimensions、weight、capacity、package_contents、features（JSON 数组）、technical_specs（JSON 对象）、target_audience、use_scenarios（JSON 数组）、unique_selling_points（JSON 数组）、brand、en_search_keywords（JSON 数组）、created_at\n   - `translations` 表：asin（主键，关联 products）、russian_title、core_keywords、russian_description、phase1_model、phase2_model、created_at\n2. `db.py` 模块提供：`init_db()`、`upsert_product()`、`get_product()`、`upsert_translation()`、`get_translation()`、`get_all_products()`、`get_products_pending_phase2()` 等接口\n3. 配置层拆分：`PHASE1_API_PROVIDER` / `PHASE1_API_KEY` / `PHASE1_API_BASE_URL` / `PHASE1_MODEL` + `PHASE2_*` 对应四字段。Phase 2 未配置时自动 fallback 到 Phase 1 值\n4. HTML 存档目录约定：`html/{ASIN}.html`，数据库 `html_path` 字段存相对路径\n\n## Acceptance criteria\n\n- [ ] 运行 `init_db()` 后可通过 sqlite3 CLI 查看完整表结构\n- [ ] 插入测试产品记录后可通过 `get_product()` 正确读出\n- [ ] `get_products_pending_phase2()` 正确返回有产品记录但无翻译记录的 ASIN 列表\n- [ ] Phase 2 配置为空时 fallback 到 Phase 1 值\n- [ ] 现有配置 `TRANSLATE_*` 保留向后兼容（Phase 1 可读取旧配置名）\n\n## Blocked by\n\nNone - can start immediately.","title":"#7 数据库与配置基础设施"}
